{
  "name": "account-portal",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run && vitest run --config vitest.static.config.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.2.5",
    "vitest": "^4.1.11",
    "wrangler": "^4.138.0"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.3.3",
    "jose": "^6.0.11"
  }
}
