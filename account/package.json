{
  "name": "account-portal",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --config vitest.config.js && vitest run --config vitest.static.config.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.41",
    "vitest": "^2.1.9",
    "wrangler": "^3.100.0"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.3.0"
  }
}
