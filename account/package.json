{
  "name": "account-portal",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:sandbox-run-contract": "node scripts/generate-sandbox-run-contract.mjs",
    "test": "node scripts/generate-sandbox-run-contract.mjs --check && node scripts/check-test-partitions.mjs && vitest run --config vitest.worker.config.js && vitest run --config vitest.passkey.config.js && vitest run --config vitest.node.config.js",
    "test:resources": "node scripts/measure-test-resources.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.41",
    "vitest": "^2.1.9",
    "wrangler": "^3.100.0"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.3.0",
    "jose": "^6.0.11"
  }
}
